package com.DataRunner.CountryTown

import androidx.appcompat.app.AppCompatActivity
import com.google.gson.Gson
import com.google.gson.reflect.TypeToken
import org.json.JSONArray

class Map : AppCompatActivity() {

//    val assetManager = resources.assets
//    val inputStream= assetManager.open("data.json")
//    val jsonString = inputStream.bufferedReader().use { it.readText() }
//    val jArray = JSONArray(jsonString)
//
//    for (i in 0 until jArray.length()) {
//        val obj = jArray.getJSONObject(i)
//    }

//    val jsonFileString = getJsonDataFromAsset(applicationContext, "bezkoder.json")
//
//    val gson = Gson()
//    val listPersonType = object : TypeToken<List<String>>() {}.type
//
//    var secrets: List<String> = gson.fromJson(jsonFileString, listPersonType)
}